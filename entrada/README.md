# Entrada do ETL

Substitua `nfs.xlsx` pela exportação mais recente do Power BI e envie a alteração para a branch `main`.

O workflow valida a planilha, concilia quantidade e valor, registra o snapshot real do dia e publica os JSONs consumidos pelo dashboard.
